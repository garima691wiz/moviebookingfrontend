/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This looks for Tailwind classes in all JS, JSX, TS, and TSX files inside the src directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
